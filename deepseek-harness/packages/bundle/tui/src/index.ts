/**
 * @deepseek-ai/dsh-tui — the interactive terminal profile bundle over
 * dsh-base. Its static patch adds only surface values and the stdio JSON-RPC
 * server; this module carries no runtime API.
 * @module @deepseek-ai/dsh-tui
 */

export {}
